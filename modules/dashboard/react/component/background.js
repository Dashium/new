import React from 'react';

const Background = () => {
    const styles = {
        background: 'linear-gradient(100deg, #0e044b, #6901df)',
        height: '100vh',
    };

    return (
        <div style={styles} />
    );
};

export default Background;
